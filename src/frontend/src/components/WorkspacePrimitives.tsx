import { ChevronRight } from 'lucide-react';
import Link from 'next/link';

export function WorkspaceHeader({ eyebrow, title, description, trust }: { eyebrow: string; title: string; description: string; trust?: React.ReactNode }) { return <header className="mb-7 flex flex-col justify-between gap-4 border-b border-white/[0.07] pb-6 md:flex-row md:items-end"><div><p className="section-eyebrow">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{description}</p></div>{trust}</header>; }
export function Breadcrumbs({ items }: { items: Array<{ label: string; href?: string }> }) { return <nav aria-label="Breadcrumb" className="mb-4 flex flex-wrap items-center gap-1 text-[10px] text-muted-foreground">{items.map((item, index) => <span key={item.label} className="flex items-center gap-1">{index > 0 && <ChevronRight className="h-3 w-3" />}{item.href ? <Link href={item.href} className="hover:text-foreground">{item.label}</Link> : item.label}</span>)}</nav>; }
export function HonestEmpty({ title, detail, action }: { title: string; detail: string; action?: string }) { return <div className="border-y border-white/[0.07] px-5 py-9 text-center"><p className="text-sm font-medium">{title}</p><p className="mx-auto mt-2 max-w-lg text-xs leading-5 text-muted-foreground">{detail}</p>{action && <p className="mt-3 text-xs font-medium text-accent">{action}</p>}</div>; }

import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
    it("should fail", () => {
    expect(false).toBe(true);
    });